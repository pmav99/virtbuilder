---

build:
  os: 'cirros'
  version: '0.3.5'
  output: '/tmp/simple.qcow2'
  format: 'qcow2'
  arch: 'x86_64'
  no-sync: false
  memsize: 4000
  smp: 4
  config:
    update: false
    selinux-relabel: false
    password-crypto: 'sha512'
